<%= content_for :title, t("shared.scaffold.new.title", model: "Webhook Endpoint") %>

<div class="container px-4 mx-auto my-8">
  <div class="flex items-center justify-between mb-4">
    <h1 class="h3">
      <%= link_to "Webhook Endpoints", webhook_endpoints_path, class: "text-black" %>
      <span class="text-gray-400 font-light mx-2">\</span>
      <%= t("shared.scaffold.new.title", model: "Webhook Endpoint") %>
    </h1>
  </div>

  <div class="p-8 bg-white rounded shadow">
    <%= render "form", webhook_endpoint: @webhook_endpoint %>
  </div>
</div>
